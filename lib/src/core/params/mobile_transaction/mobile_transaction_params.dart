class MobileTransactionParams{
  final String? accessToken;
  final String? agentMemberId;
  final List<String>? memberUnRemittedDonations;

  MobileTransactionParams({this.accessToken, this.agentMemberId, this.memberUnRemittedDonations});
}